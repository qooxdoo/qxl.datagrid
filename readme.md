# qxl.datagrid -

DataGrid is a Qooxdoo UI component that a virtual, scrolling, spreadsheet-like control with collapsable groups (either for
grouping or to act as a tree); it includes an underlying API (independently from the UI component) for obtaining data on
demand asynchronously (eg from a server)
